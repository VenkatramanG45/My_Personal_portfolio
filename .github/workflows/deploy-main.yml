# Production Deployment Workflow
name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write
      issues: write
      statuses: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Create .npmrc file
        run: echo "legacy-peer-deps=true" > .npmrc

      - name: Install dependencies
        run: npm install

      - name: List installed packages (debug)
        run: npm list react-tilt || echo "react-tilt not found"

      - name: Check node_modules (debug)
        run: ls -la node_modules/ | grep react-tilt || echo "react-tilt directory not found"

      - name: Build project
        run: npm run build

      - name: Verify build output
        run: ls -la dist/

      - name: Deploy to Netlify Production
        uses: nwtgck/actions-netlify@v3.0
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        with:
          publish-dir: './dist'
          production-branch: main
          production-deploy: true
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Production deploy from main branch"
          enable-commit-comment: false
          enable-pull-request-comment: false
